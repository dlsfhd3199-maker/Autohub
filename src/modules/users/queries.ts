import{requireOrganizationAdmin}from"@/modules/authorization/server";export async function getUserManagementData(){const{supabase,organizationId}=await requireOrganizationAdmin();const[applications,brands,statuses]=await Promise.all([supabase.from("membership_applications").select("id,user_id,requested_role,organization_name,brand_name,storefront_url,applicant_name,job_title,phone,email_normalized,joined_on,status,safe_reason,created_at,reviewed_at").eq("requested_role","ae").order("created_at",{ascending:false}),supabase.from("brands").select("id,name").eq("agency_organization_id",organizationId).is("archived_at",null).order("name"),supabase.from("user_account_statuses").select("user_id,status,safe_reason,reviewed_at")]);const error=applications.error??brands.error??statuses.error;if(error)throw new Error("가입 신청 목록을 불러오지 못했습니다.");return{applications:applications.data??[],brands:brands.data??[],statuses:statuses.data??[]}}
