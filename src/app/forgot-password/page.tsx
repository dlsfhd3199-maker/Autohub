import Link from "next/link";import { requestPasswordReset } from "@/modules/registration/actions";
export default function ForgotPasswordPage(){return <main className="account-status-page"><section className="auth-card"><p className="eyebrow">PASSWORD RESET</p><h1>비밀번호 찾기</h1><p>계정이 존재하는지 노출하지 않고 동일한 안내를 제공합니다.</p><form action={requestPasswordReset}><label className="field"><span>이메일</span><input name="email" type="email" autoComplete="email" required/></label><button className="primary-button">재설정 안내 요청</button></form><Link className="back-link" href="/login">← 로그인으로 돌아가기</Link></section></main>}
