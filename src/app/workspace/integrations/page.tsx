import Link from"next/link";import{listAccessibleBrands}from"@/modules/brands/queries";export default async function Page(){const brands=await listAccessibleBrands();return <main className="workspace-content"><section className="page-heading"><div><p className="eyebrow">INTEGRATIONS</p><h1>연동 관리</h1><p>플랫폼 연결을 확인할 Virtual 브랜드를 선택하세요.</p></div></section><div className="brand-grid">{brands.map((brand)=><Link className="brand-card panel" href={`/workspace/brands/${brand.id}/integrations`} key={brand.id}><h2>{brand.name}</h2><p>{brand.domain}</p><span className="card-link">플랫폼 연결 보기 →</span></Link>)}</div></main>}
