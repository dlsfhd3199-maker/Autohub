import Link from "next/link";import{Icon}from"@/components/icons";import{listGenerationProviders}from"@/modules/ai-generation/provider";import{getWorkspaceContext}from"@/modules/auth/workspace";import{listAccessibleBrands}from"@/modules/brands/queries";
export default async function Page(){const[context,brands]=await Promise.all([getWorkspaceContext(),listAccessibleBrands()]);if(context.role!=="agency_admin")return <main className="workspace-content"><section className="state-card"><Icon name="warning"/><h1>시스템 AI 공급자 설정 권한이 없습니다</h1><p>브랜드 콘텐츠 설정과 시스템 공급자 활성화는 분리되어 있습니다.</p><Link className="secondary-button" href="/workspace/brands">브랜드 보기</Link></section></main>;return <main className="workspace-content"><section className="page-heading"><div><p className="eyebrow">SYSTEM AI PROVIDERS</p><h1>AI 공급자 설정</h1><p>API 키나 비밀값 없이 서버의 안전 상태만 표시합니다.</p></div></section><div className="connector-grid">{listGenerationProviders().map((provider)=><article className="connector-card panel" key={provider.id}><header><Icon name="ai"/><div><h2>{provider.label}</h2><span className={`status-badge ${provider.status.enabled?"status-active":"status-archived"}`}>{provider.status.enabled?"사용 가능":"비활성"}</span></div></header><p>{provider.description}</p><dl><div><dt>설정</dt><dd>{provider.status.configured?"구성됨":"구성 안 됨"}</dd></div><div><dt>외부 네트워크</dt><dd>{provider.status.networkAllowed?"명시적 허용":"차단"}</dd></div><div><dt>상태 코드</dt><dd>{provider.status.safeCode}</dd></div></dl><div className="capability-list">{provider.status.capabilities.map((item)=><span key={item}>{item}</span>)}</div></article>)}</div><section className="panel section-panel"><h2>브랜드 AI 콘텐츠 설정</h2><p>브랜드 소개·말투·금지 표현·가상 상품·공식 근거는 브랜드 단위로 관리합니다.</p><div className="inline-links">{brands.map((brand)=><Link className="secondary-button" href={`/workspace/brands/${brand.id}/ai-settings`} key={brand.id}>{brand.name}</Link>)}</div></section></main>}
