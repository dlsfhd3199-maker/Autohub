import Link from"next/link";import{Icon}from"@/components/icons";import{getWorkspaceContext}from"@/modules/auth/workspace";import{getBrand}from"@/modules/brands/queries";import{getSafeConnectionSummaries}from"@/modules/publishing/management";import{listPublishingConnectors}from"@/modules/publishing-connectors/registry";
const labels:Record<string,string>={"local-test-store":"로컬 테스트 자사몰","html-export":"HTML 내보내기",cafe24:"Cafe24","custom-api":"Custom API"};export default async function Page({params}:{params:Promise<{brandId:string}>}){const{brandId}=await params;const[brand,context,summaries]=await Promise.all([getBrand(brandId),getWorkspaceContext(),getSafeConnectionSummaries(brandId)]);const byProvider=new Map(summaries.map((item)=>[item.provider,item]));return <main className="workspace-content"><Link className="back-link" href={`/workspace/brands/${brandId}`}>← {brand.name}</Link><section className="page-heading"><div><p className="eyebrow">BRAND INTEGRATIONS</p><h1>연동 관리</h1><p>{context.role==="agency_admin"?"연결 상태와 지원 기능을 관리합니다.":"권한에 허용된 안전한 연결 요약만 표시합니다."}</p></div></section><div className="connector-grid">{listPublishingConnectors().map((connector)=>{const summary=byProvider.get(connector.id);const cafe=connector.id==="cafe24";return <article className="connector-card panel" key={connector.id}><header><Icon name="integration"/><div><h2>{labels[connector.id]}</h2><span className={`status-badge ${summary?.status==="connected"?"status-active":"status-archived"}`}>{summary?.status==="connected"?"연결됨":cafe?"설치 전":"연결 안 됨"}</span></div></header><p>{cafe?"연결 준비 중이며 실제 카페24와 연결되지 않았습니다. OAuth와 API 요청을 수행하지 않습니다.":connector.id==="local-test-store"?"현재 로컬 테스트몰과 서버 전용 키로 연결됩니다.":"플랫폼 독립 발행 계약을 위한 안전한 연결 항목입니다."}</p><div className="capability-list">{connector.capabilities.length?connector.capabilities.map((item)=><span key={item}>{item}</span>):<span>지원 기능 준비 중</span>}</div><dl><div><dt>마지막 점검</dt><dd>{summary?.lastVerifiedAt?new Date(summary.lastVerifiedAt).toLocaleString("ko-KR"):"아직 점검하지 않음"}</dd></div><div><dt>공개 도메인</dt><dd>{summary?.publicDomain??"설정 안 됨"}</dd></div></dl>{cafe&&<button className="secondary-button" disabled>카페24 커넥터 설치 전</button>}</article>})}</div></main>}
