import { Icon } from "@/components/icons";import { LoginForm } from "@/components/login-form";
export const dynamic="force-dynamic";export const revalidate=0;
export default async function LoginPage({searchParams}:{searchParams:Promise<{error?:string;reset?:string}>}){const params=await searchParams;return <main className="auth-page"><section className="auth-brand-panel" aria-label="AEO Content Hub 소개"><div className="brand-lockup auth-lockup"><span className="brand-mark">A</span><span><strong>AEO Content Hub</strong><small>콘텐츠 운영 워크스페이스</small></span></div><div><p className="eyebrow">CONTENT OPERATIONS</p><h2>브랜드 지식부터 콘텐츠 발행까지<br/>하나의 워크스페이스에서</h2><p>관리자·마케터·광고주에게 필요한 업무와 권한을 안전하게 분리합니다.</p></div><ul><li><Icon name="brand"/>브랜드별 데이터 격리</li><li><Icon name="people"/>역할과 담당 브랜드 관리</li><li><Icon name="history"/>자동 저장과 버전 이력</li></ul></section><section className="auth-form-panel"><div className="auth-card" aria-labelledby="login-title"><p className="eyebrow">WELCOME BACK</p><h1 id="login-title">워크스페이스 로그인</h1><p>승인된 관리자, 마케터 또는 광고주 계정으로 로그인하세요.</p><LoginForm error={params.error} resetRequested={params.reset==="requested"}/></div></section></main>}
