import{redirect}from"next/navigation";import{InitialPasswordForm}from"@/components/initial-password-form";import{createClient}from"@/lib/supabase/server";export const dynamic="force-dynamic";export default async function ChangePasswordPage(){const supabase=await createClient();const claims=await supabase.auth.getClaims();if(!claims.data?.claims?.sub)redirect("/login");const{data}=await supabase.from("user_account_statuses").select("status,password_change_required").eq("user_id",claims.data.claims.sub).maybeSingle();if(data?.status!=="approved")redirect(`/account-status?status=${data?.status??"pending"}`);if(!data.password_change_required)redirect("/workspace");return <main className="account-status-page"><section className="auth-card"><p className="eyebrow">SECURE ACCOUNT</p><h1>비밀번호를 변경해 주세요</h1><p>임시 비밀번호는 최초 로그인에만 사용됩니다. 새 비밀번호를 설정하면 본인 브랜드 워크스페이스로 이동합니다.</p><InitialPasswordForm/></section></main>}
