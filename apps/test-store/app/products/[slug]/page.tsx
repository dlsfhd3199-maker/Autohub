import type { Metadata } from "next";import Link from "next/link";import { notFound } from "next/navigation";import { virtualOrganization,virtualProducts } from "../../../lib/catalog";
export function generateStaticParams(){return virtualProducts.map(({slug})=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const product=virtualProducts.find((item)=>item.slug===slug);return product?{title:product.name,description:product.description,alternates:{canonical:`https://example.com/products/${product.slug}`}}:{}}
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const product=virtualProducts.find((item)=>item.slug===slug);if(!product)notFound();const productJsonLd={"@context":"https://schema.org","@type":"Product",name:product.name,description:product.description,image:[product.image],brand:{"@type":"Brand",name:virtualOrganization.name},offers:{"@type":"Offer",price:String(product.price),priceCurrency:product.currency,availability:product.availability,url:`https://example.com/products/${product.slug}`},aggregateRating:{"@type":"AggregateRating",ratingValue:String(product.rating),reviewCount:String(product.reviewCount)}};const breadcrumbs={"@context":"https://schema.org","@type":"BreadcrumbList",itemListElement:[{"@type":"ListItem",position:1,name:"홈",item:"https://example.com/"},{"@type":"ListItem",position:2,name:"가상 상품",item:"https://example.com/products"},{"@type":"ListItem",position:3,name:product.name,item:`https://example.com/products/${product.slug}`}]};return <main><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(productJsonLd)}}/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(breadcrumbs)}}/><article className="store-info-page"><nav className="breadcrumbs" aria-label="현재 위치"><Link href="/">홈</Link><span>/</span><Link href="/products">가상 상품</Link><span>/</span><span>{product.name}</span></nav><p className="eyebrow">{product.category}</p><h1>{product.name}</h1><p>{product.description}</p><dl><div><dt>가격</dt><dd>{product.price.toLocaleString("ko-KR")}원</dd></div><div><dt>재고</dt><dd>{product.availability.endsWith("InStock")?"재고 있음":"한정 수량"}</dd></div><div><dt>평점</dt><dd>{product.rating} · 가상 리뷰 {product.reviewCount}건</dd></div></dl><p><a href={`https://example.com/products/${product.slug}`}>가상 상품 공식 URL</a></p></article></main>}
